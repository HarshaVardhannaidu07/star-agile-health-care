apiVersion: v1
kind: Service
metadata:
  name: medicure-service
spec:
  type: NodePort
  selector:
    app: medicure
  ports:
    - port: 8081        # Service port
      targetPort: 8081  # Container port
      nodePort: 32010   # NodePort (choose between 30000-32767)
